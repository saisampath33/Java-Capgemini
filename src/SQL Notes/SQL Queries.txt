SQL Queries

SET PAGES 100 LINES 100 //FORMATTING THE DOCUMENT

select ename,sal from emp where sal>2000;

select * from emp where ename = 'SMITH';

select ename,hiredata from emp where hiredata>'31-DEC-82'

select emp.*,sal*12 as annual_salary from emp where sal*12 > 18000;

SELECT * FROM EMP WHERE JOB IN ('MANAGER','ANALYST') AND DEPTNO IN (10,20); 

SELECT * FROM EMP WHERE SAL<3000 AND DEPTNO IN (10,30);    //IN

SELECT ENAME,JOB FROM EMP WHERE JOB NOT IN ('SALESMAN','CLERK');	//NOT IN

SELECT ENAME,SAL FROM EMP WHERE SAL BETWEEN 1500 AND 3000; //BETWEEN OPERATOR RANGE OF 1500 AND 3000

SELECT ENAME,SAL FROM EMP WHERE SAL BETWEEN 1501 AND 2999; // BETWEEN OPERATOR FOR LESS THN 3000 AND MORE THAN 1500

SELECT ENAME,HIREDATE FROM EMP WHERE HIREDATE BETWEEN '01-JAN-82' AND '31-DEC-88';

