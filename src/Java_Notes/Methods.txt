Methods

A block of code that perform a specific tasks
Method signature = Method name + Formal Arguments
Method Declaration = Modifers return type and method signature
Method definition = method declaration + method body 

			Modifiers -> used to change the behaviour of the method

Access Modifiers			Non-Access Modifier
Public 					Static
Protected				Abstract
Private					Final
default(no need to provide the keyword default)

Return Type: will return the data or it wont return anything
If returns -> primitive type(8 Types)  and non primitive type(Any Classname) and void(Not returning anything)


modifier − It defines the access type of the method and it is optional to use.

returnType − Method may return a value.

nameOfMethod − This is the method name. The method signature consists of the method name and the parameter list.

Parameter List − The list of parameters, it is the type, order, and number of parameters of a method. These are optional, method may contain zero parameters.

method body − The method body defines what the method does with the statements.

1)Creating inside the Class 
2)We cannot create a method inside a method

modifier returnType nameOfMethod (Parameter List) {
   // method body
}