Strings1 By using string literalString s = "Hello" stored in string constant pool areaIn heap area one more area that is string constant pool area here one object will be created hereIf the same literal already exists, no new object is created.Reference points to the existing objects1 and s2 point to the same object in SCP.indexOf() is a String method."aeiou".indexOf(ch) != -1It checks whether a character exists in the string or not.If found → returns the index positionIf NOT found → returns -12 By using new keywordCreates two objectsOne in String Constant Pool (if not already present)One in Heap memoryCreates new object every timeString s = new String();We have String constructorString is immutability -> Immutability means once an object is created, it cannot be changed.concat() creates a new String object, it does NOT modify previous stringUsed for security,thread safety(Multiple threads can share same String safely),saves memoryString bufferMutableSame version of java they introduced string class and string bufferAll methods are synchronizedStringBuffer s = new StringBuffer();Created at regular heap areaIf we modify anything no new memory block will not create like string class1 Append2 insert3 Reverse4 Replace5 Delete6 Capacity - by default buffercapacity is 16(old capacity * 2) + 2 -> This much it will expand after 16 char filled 16*2+2 = 34To achieve Multi Threading we use thisSlowString Builder (java 1.5)Same methodsFastNo multithreadingThread Safety and Multi Threading